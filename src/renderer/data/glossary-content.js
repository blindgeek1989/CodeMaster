const glossaryData = [
  // ===== HTML =====
  { term: 'Element', definition: 'The fundamental building block of HTML. An element consists of an opening tag, optional content, and a closing tag: <p>Hello</p>. Some elements are self-closing: <img />.', module: 'HTML', example: '<p>This is a paragraph element.</p>' },
  { term: 'Tag', definition: 'The angle-bracket markers that surround an element\'s name. Opening tags look like <p>; closing tags include a slash: </p>.', module: 'HTML', example: '<h1>Opening tag — H1 Closing tag</h1>' },
  { term: 'Attribute', definition: 'Extra information added to an opening tag as a name="value" pair. Attributes modify or describe the element.', module: 'HTML', example: '<a href="https://example.com">Link</a>' },
  { term: 'Semantic HTML', definition: 'Using HTML elements that describe the meaning of content, not just its appearance. <article>, <nav>, <main>, and <button> are semantic; <div> and <span> are not.', module: 'HTML', example: '<nav aria-label="Main navigation">...</nav>' },
  { term: 'DOCTYPE', definition: 'The declaration at the very top of an HTML file that tells the browser which version of HTML to use. Modern pages use <!DOCTYPE html>.', module: 'HTML', example: '<!DOCTYPE html>' },
  { term: 'Heading', definition: 'HTML elements h1 through h6 that create a document outline. h1 is the most important (page title); h6 is the least. Screen readers use headings to navigate pages.', module: 'HTML', example: '<h1>Page Title</h1>\n<h2>Section</h2>' },
  { term: 'Anchor', definition: 'The <a> element, used to create hyperlinks. The href attribute specifies the destination. Without href, an anchor is not keyboard focusable.', module: 'HTML', example: '<a href="/about">About us</a>' },
  { term: 'Alt text', definition: 'A text alternative for images, written in the alt attribute. Screen readers read alt text aloud. Use alt="" for decorative images.', module: 'HTML', example: '<img src="logo.png" alt="CodeMaster logo" />' },
  { term: 'Form label', definition: 'A <label> element associated with a form input. Clicking the label focuses the input. Essential for screen readers — without a label, inputs are announced as "edit text" with no description.', module: 'HTML', example: '<label for="email">Email</label>\n<input type="email" id="email" />' },
  { term: 'Landmark', definition: 'An ARIA role or HTML5 element (header, nav, main, footer, aside, section) that divides a page into named regions. Screen reader users can jump between landmarks to navigate quickly.', module: 'HTML', example: '<main id="main-content">...</main>' },
  { term: 'Tab order', definition: 'The sequence in which keyboard focus moves through interactive elements when the user presses Tab. Tab order follows the HTML source order by default. Never use tabindex > 0 — it disrupts the natural order.', module: 'HTML', example: '<!-- Natural order: Tab moves link → button → input -->' },
  { term: 'Skip link', definition: 'A visually hidden link at the very top of the page that allows keyboard and screen reader users to jump past repeated navigation and go straight to the main content.', module: 'HTML', example: '<a class="skip-link" href="#main-content">Skip to main content</a>' },

  // ===== CSS =====
  { term: 'Selector', definition: 'The part of a CSS rule that identifies which HTML elements to style. Examples: p selects all paragraphs; .card selects elements with class "card"; #hero selects the element with id "hero".', module: 'CSS', example: 'p { color: #333; }' },
  { term: 'Cascade', definition: 'The algorithm CSS uses to determine which style wins when multiple rules apply to the same element. Rules are evaluated by origin, specificity, and order.', module: 'CSS', example: '/* Later rules override earlier ones at equal specificity */' },
  { term: 'Specificity', definition: 'A weight assigned to a CSS selector that determines which rule takes priority. IDs beat classes; classes beat element selectors. Inline styles beat them all.', module: 'CSS', example: '#header { color: red; } /* beats */ .header { color: blue; }' },
  { term: 'Box model', definition: 'The layout model where every element is a rectangular box with four layers: content, padding (inside the border), border, and margin (outside the border).', module: 'CSS', example: 'box-sizing: border-box; /* includes padding/border in width */' },
  { term: 'Flexbox', definition: 'A CSS layout system that arranges items in a single row or column. The parent container has display: flex; child items can grow, shrink, and align automatically.', module: 'CSS', example: '.container { display: flex; gap: 1rem; align-items: center; }' },
  { term: 'Grid', definition: 'A CSS layout system that arranges items in rows AND columns simultaneously. More powerful than Flexbox for two-dimensional layouts.', module: 'CSS', example: '.grid { display: grid; grid-template-columns: repeat(3, 1fr); }' },
  { term: 'Media query', definition: 'A CSS rule that applies styles only when a condition is met — typically a screen width range. Used to build responsive layouts.', module: 'CSS', example: '@media (min-width: 768px) { .sidebar { display: block; } }' },
  { term: 'Custom property', definition: 'A CSS variable declared with -- and used with var(). Lets you define a value once and reuse it throughout the stylesheet. Changes in one place update everywhere.', module: 'CSS', example: ':root { --color-accent: #6366f1; }\na { color: var(--color-accent); }' },
  { term: 'Pseudo-class', definition: 'A keyword added to a selector with : that targets an element in a specific state. :hover targets on mouse hover; :focus targets keyboard/mouse focus; :checked targets a checked checkbox.', module: 'CSS', example: 'button:hover { background: #4f46e5; }' },
  { term: ':focus-visible', definition: 'A CSS pseudo-class that applies styles only when an element is focused via keyboard, not mouse click. Use it instead of :focus to show focus rings only for keyboard users without affecting mouse users.', module: 'CSS', example: 'button:focus-visible { outline: 3px solid #f59e0b; }' },
  { term: 'rem / em', definition: 'Relative CSS length units. 1rem = root font size (usually 16px). 1em = parent font size. Prefer rem for font sizes so users can zoom text in browser settings.', module: 'CSS', example: 'h1 { font-size: 2rem; } /* 32px if root is 16px */' },
  { term: 'sr-only', definition: 'A CSS utility class that hides content visually but keeps it readable by screen readers. Uses position: absolute with 1x1 pixel dimensions and overflow: hidden — NOT display: none.', module: 'CSS', example: '.sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; }' },
  { term: 'prefers-reduced-motion', definition: 'A CSS media query that detects when the user has requested reduced animation in their OS settings. Always wrap animations in a check for this preference.', module: 'CSS', example: '@media (prefers-reduced-motion: reduce) { * { animation: none; } }' },

  // ===== JavaScript =====
  { term: 'Variable', definition: 'A named container for a value. In modern JavaScript, declare with const (value never reassigned) or let (value may change). Avoid var.', module: 'JavaScript', example: 'const name = "Alice";\nlet score = 0;' },
  { term: 'Function', definition: 'A reusable block of code with a name. Call it by writing the name followed by parentheses. Functions can accept parameters and return values.', module: 'JavaScript', example: 'function greet(name) {\n  return `Hello, ${name}!`;\n}' },
  { term: 'Event listener', definition: 'A function attached to an element that runs when a specific event occurs (click, keydown, submit, focus). Added with addEventListener().', module: 'JavaScript', example: 'button.addEventListener("click", () => {\n  console.log("Clicked!");\n});' },
  { term: 'DOM', definition: 'Document Object Model. The browser\'s tree representation of the HTML page. JavaScript uses the DOM to read and change page content dynamically.', module: 'JavaScript', example: 'const h1 = document.querySelector("h1");\nh1.textContent = "New title";' },
  { term: 'Promise', definition: 'An object representing a future value — the result of an asynchronous operation that hasn\'t finished yet. Use .then() for success and .catch() for errors, or use async/await syntax.', module: 'JavaScript', example: 'fetch(url)\n  .then(r => r.json())\n  .then(data => console.log(data));' },
  { term: 'async / await', definition: 'Syntax for working with Promises that reads like synchronous code. async marks a function as asynchronous; await pauses inside it until a Promise resolves.', module: 'JavaScript', example: 'async function load() {\n  const data = await fetch(url).then(r => r.json());\n  console.log(data);\n}' },
  { term: 'Scope', definition: 'The region of code where a variable is accessible. Variables declared with const or let inside a block {} are not visible outside that block (block scope).', module: 'JavaScript', example: 'if (true) {\n  const x = 5; // only visible inside this block\n}' },
  { term: 'Try / catch', definition: 'A construct for handling errors without crashing the program. Code in try runs normally; if it throws an error, the catch block runs instead.', module: 'JavaScript', example: 'try {\n  const data = JSON.parse(text);\n} catch (e) {\n  console.error("Invalid JSON", e);\n}' },
  { term: 'Local Storage', definition: 'A browser API for storing key-value pairs persistently on the user\'s device. Data survives page refresh and browser restart. Use setItem(), getItem(), and removeItem().', module: 'JavaScript', example: 'localStorage.setItem("theme", "dark");\nconst theme = localStorage.getItem("theme");' },
  { term: 'aria-live region', definition: 'An HTML element with aria-live="polite" or aria-live="assertive" that screen readers monitor for text changes. When its content changes, the new text is announced automatically — essential for dynamic updates.', module: 'JavaScript', example: '<div aria-live="polite" id="status"></div>\n// Then in JS:\ndocument.getElementById("status").textContent = "Loading complete.";' },

  // ===== Python =====
  { term: 'Variable (Python)', definition: 'A named container for a value, created by writing name = value. Python figures out the type automatically. Variables can be reassigned at any time.', module: 'Python', example: 'name = "Alice"\nage = 30\npi = 3.14' },
  { term: 'Function (Python)', definition: 'A reusable block of code defined with def. Functions can take parameters and use return to send a value back. Indented code belongs to the function body.', module: 'Python', example: 'def add(a, b):\n    return a + b\n\nresult = add(3, 5)  # 8' },
  { term: 'List', definition: 'An ordered, mutable collection of values in Python. Created with square brackets []. Access items by index starting at 0. Use .append() to add, .remove() to delete.', module: 'Python', example: 'fruits = ["apple", "banana", "cherry"]\nfruits.append("mango")\nprint(fruits[0])  # apple' },
  { term: 'Dictionary', definition: 'An unordered collection of key-value pairs in Python. Created with curly braces {}. Access values by key name. Keys must be unique.', module: 'Python', example: 'person = {"name": "Alice", "age": 30}\nprint(person["name"])  # Alice' },
  { term: 'f-string', definition: 'A Python string that embeds variables or expressions directly by prefixing with f and using {} around variables. Cleaner than string concatenation.', module: 'Python', example: 'name = "Alice"\nage = 30\nprint(f"My name is {name} and I am {age}.")' },
  { term: 'Exception', definition: 'An error object that Python raises when it cannot execute a line. Caught with try/except. Common types: ValueError, TypeError, KeyError, IndexError, FileNotFoundError.', module: 'Python', example: 'try:\n    int("abc")\nexcept ValueError as e:\n    print(f"Error: {e}")' },
  { term: 'for loop (Python)', definition: 'Iterates over each item in a sequence (list, range, string, dictionary). The variable after for takes each value in turn.', module: 'Python', example: 'for i in range(5):\n    print(i)  # 0, 1, 2, 3, 4' },
  { term: 'Indentation (Python)', definition: 'Python uses indentation (spaces at the start of a line) to define code blocks — if bodies, function bodies, loop bodies. Incorrect indentation causes a syntax error.', module: 'Python', example: 'if True:\n    print("Indented — inside the if block")' },

  // ===== SQL =====
  { term: 'Database', definition: 'An organised collection of structured data, stored and accessed electronically. A relational database (like SQLite) organises data into tables.', module: 'SQL', example: '-- A database might have tables: users, products, orders' },
  { term: 'Table', definition: 'A structured set of data in a database, organised into rows (records) and columns (fields). Similar to a spreadsheet tab.', module: 'SQL', example: 'CREATE TABLE employees (id INTEGER, name TEXT, salary REAL);' },
  { term: 'Column', definition: 'A named field in a database table. Every column has a data type (TEXT, INTEGER, REAL, BLOB). All rows in a table share the same columns.', module: 'SQL', example: '-- Columns in an employees table: id, name, department, salary' },
  { term: 'Row', definition: 'A single record in a database table. Each row contains one value per column. Also called a "record" or "tuple".', module: 'SQL', example: '-- A row: (1, "Alice", "Engineering", 85000)' },
  { term: 'Query', definition: 'A question or instruction sent to the database. SELECT queries retrieve data; INSERT, UPDATE, and DELETE queries modify data.', module: 'SQL', example: 'SELECT name FROM employees WHERE salary > 50000;' },
  { term: 'Primary key', definition: 'A column (or combination of columns) that uniquely identifies each row. No two rows can have the same primary key value. Often an auto-incrementing integer id.', module: 'SQL', example: 'CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT);' },
  { term: 'JOIN', definition: 'A SQL clause that combines rows from two or more tables based on a related column. INNER JOIN returns only rows with matches in both tables. LEFT JOIN returns all rows from the left table.', module: 'SQL', example: 'SELECT e.name, d.name\nFROM employees e\nINNER JOIN departments d ON e.dept_id = d.id;' },
  { term: 'Aggregate function', definition: 'A SQL function that performs a calculation on a set of rows and returns a single value. Common aggregates: COUNT(), SUM(), AVG(), MIN(), MAX(). Used with GROUP BY.', module: 'SQL', example: 'SELECT department, COUNT(*) AS headcount\nFROM employees\nGROUP BY department;' },
  { term: 'WHERE clause', definition: 'Filters rows in a query. Only rows where the condition is true are returned or affected. Used with SELECT, UPDATE, and DELETE.', module: 'SQL', example: 'SELECT * FROM employees WHERE salary > 60000 AND department = "Engineering";' },
  { term: 'NULL', definition: 'A special marker in SQL meaning "no value" or "unknown". NULL is not the same as zero or an empty string. Use IS NULL / IS NOT NULL to check for it.', module: 'SQL', example: 'SELECT * FROM employees WHERE manager_id IS NULL;' },

  // ===== PowerShell =====
  { term: 'Cmdlet', definition: 'A lightweight PowerShell command following the Verb-Noun naming convention. Examples: Get-ChildItem, Set-Location, New-Item, Remove-Item. PowerShell also supports Unix-like aliases: ls, cd, rm.', module: 'PowerShell', example: 'Get-ChildItem  # list files and folders' },
  { term: 'Pipeline', definition: 'The | operator that sends the output of one command as input to the next. PowerShell pipelines pass objects, not just text, making filtering and transformation powerful.', module: 'PowerShell', example: 'Get-ChildItem | Where-Object { $_.Extension -eq ".txt" }' },
  { term: 'Variable (PowerShell)', definition: 'Named containers for values in PowerShell, always prefixed with $. Assigned with =. Can hold any type.', module: 'PowerShell', example: '$name = "Alice"\n$age = 30\nWrite-Output "Hello, $name"' },
  { term: 'Alias', definition: 'A short name for a cmdlet. PowerShell includes built-in aliases: ls (Get-ChildItem), cd (Set-Location), cat (Get-Content), rm (Remove-Item), echo (Write-Output).', module: 'PowerShell', example: 'ls  # same as Get-ChildItem' },
  { term: 'Path', definition: 'A string describing the location of a file or folder. Absolute paths start from the drive root (C:\\Users\\). Relative paths start from the current location (.\\subfolder\\).', module: 'PowerShell', example: 'Set-Location "C:\\Users\\Alice\\Documents"' },
  { term: 'Redirection', definition: 'Operators that send output to a file instead of the screen. > writes (overwrites), >> appends. < reads input from a file.', module: 'PowerShell', example: 'Write-Output "Hello" > hello.txt\nWrite-Output "World" >> hello.txt' },
  { term: 'Script', definition: 'A text file with a .ps1 extension containing a sequence of PowerShell commands. Run with .\\script.ps1 from the terminal.', module: 'PowerShell', example: '# hello.ps1\nWrite-Output "Hello from a script!"' },

  // ===== Accessibility (cross-module) =====
  { term: 'Accessibility', definition: 'The practice of designing products usable by people with a wide range of abilities, including those who use screen readers, keyboard-only navigation, or other assistive technologies.', module: 'Accessibility', example: '<!-- Every image needs alt text, every input needs a label -->' },
  { term: 'Screen reader', definition: 'Software that reads on-screen content aloud. Examples: NVDA (Windows, free), JAWS (Windows), VoiceOver (macOS/iOS), TalkBack (Android). Screen readers navigate via headings, landmarks, and links.', module: 'Accessibility', example: '<!-- Screen readers announce element role, name, and state -->' },
  { term: 'WCAG', definition: 'Web Content Accessibility Guidelines. The international standard for web accessibility, published by the W3C. Level AA is the most widely required standard.', module: 'Accessibility', example: '<!-- WCAG 2.1 AA: 4.5:1 contrast ratio for normal text -->' },
  { term: 'Contrast ratio', definition: 'A number from 1:1 (no contrast) to 21:1 (maximum contrast) measuring the brightness difference between text and background. WCAG AA requires 4.5:1 for normal text, 3:1 for large text.', module: 'Accessibility', example: '/* #ffffff on #6366f1 ≈ 4.55:1 — passes WCAG AA */' },
  { term: 'ARIA', definition: 'Accessible Rich Internet Applications. A set of HTML attributes (aria-label, aria-expanded, aria-live, role) that add semantic meaning to elements for assistive technologies. Use native HTML first; ARIA when needed.', module: 'Accessibility', example: '<button aria-expanded="false" aria-controls="menu">Menu</button>' },
  { term: 'Focus management', definition: 'Programmatically moving keyboard focus when dynamic content changes. When a modal opens, focus must move into it. When it closes, focus must return to the trigger element.', module: 'Accessibility', example: 'modal.querySelector("[data-focus-first]").focus();' },
  { term: 'Keyboard navigation', definition: 'Operating a UI using only the keyboard. Tab moves between focusable elements; Enter/Space activate buttons; arrow keys navigate within components (menus, tabs). Everything a mouse can do, a keyboard must also be able to do.', module: 'Accessibility', example: '<!-- All interactive elements must be reachable and operable by keyboard -->' },
];

if (typeof module !== 'undefined') module.exports = glossaryData;
if (typeof window !== 'undefined') window.glossaryData = glossaryData;
